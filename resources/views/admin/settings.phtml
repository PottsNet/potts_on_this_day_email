<?php

declare(strict_types=1);

use Fisharebest\Webtrees\Tree;

/**
 * @var string $action_url
 * @var string $control_panel_url
 * @var string $module_name
 * @var array<int,Tree> $trees
 * @var array<string,string> $tree_urls
 * @var Tree|null $selected_tree
 * @var array<string,string> $settings
 * @var bool $saved
 * @var bool $prepared
 * @var bool $token_reset
 * @var string $error
 * @var string $version
 * @var string $subscriber_html
 * @var string $scheduler_html
 * @var string $return_url
 */

$selected_name = $selected_tree instanceof Tree ? $selected_tree->name() : '';
$sender_name = (string) ($settings['sender_name'] ?? '');
$sender_email = (string) ($settings['sender_email'] ?? '');
$timezone = (string) ($settings['timezone'] ?? 'UTC');
$has_token = (string) ($settings['token'] ?? '') !== '';
?>

<div class="container-xl py-3">
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="<?= e($control_panel_url) ?>">Control panel</a></li>
            <li class="breadcrumb-item active" aria-current="page">Potts On This Day Email settings</li>
        </ol>
    </nav>

    <?php if ($saved) : ?>
        <div class="alert alert-success" role="status">The Potts On This Day Email settings have been saved.</div>
    <?php endif ?>
    <?php if ($prepared) : ?>
        <div class="alert alert-success" role="status">The secure scheduler link has been prepared.</div>
    <?php endif ?>
    <?php if ($token_reset) : ?>
        <div class="alert alert-warning" role="status">A new scheduler token has been generated. Update the scheduled task because the previous URL will no longer work.</div>
    <?php endif ?>
    <?php if ($error !== '') : ?>
        <div class="alert alert-danger" role="alert"><?= e($error) ?></div>
    <?php endif ?>

    <div class="d-flex flex-wrap align-items-start justify-content-between gap-3 mb-4">
        <div>
            <h2 class="h3 mb-1">Potts On This Day Email settings</h2>
            <p class="text-muted mb-0">Configure webtrees email delivery, the family tree and the daily scheduler.</p>
        </div>
        <div class="d-flex flex-wrap align-items-center justify-content-end gap-2">
            <?php if ($return_url !== '') : ?>
                <a class="btn btn-outline-secondary btn-sm" href="<?= e($return_url) ?>">Return to previous page</a>
            <?php endif ?>
            <a class="btn btn-outline-secondary btn-sm" href="<?= e($control_panel_url) ?>">Control panel</a>
            <span class="badge text-bg-secondary">v<?= e($version) ?></span>
        </div>
    </div>

    <?php if ($trees === []) : ?>
        <div class="alert alert-warning">No family trees are available. Create or import a tree before configuring this module.</div>
    <?php else : ?>
        <div class="card mb-3">
            <div class="card-header"><h3 class="h5 mb-0">Family tree</h3></div>
            <div class="card-body">
                <label class="form-label" for="potts-otd-tree-select">Configure this tree</label>
                <select class="form-select" id="potts-otd-tree-select" onchange="window.location.href=this.value">
                    <?php foreach ($trees as $tree) : ?>
                        <?php $tree_url = $tree_urls[$tree->name()] ?? route('module', ['module' => $module_name, 'action' => 'Admin']); ?>
                        <option value="<?= e($tree_url) ?>" <?= $tree->name() === $selected_name ? 'selected' : '' ?>><?= e($tree->title()) ?></option>
                    <?php endforeach ?>
                </select>
                <div class="form-text">Subscriber status and scheduler URLs are shown for the selected tree.</div>
            </div>
        </div>

        <form method="post" action="<?= e($action_url) ?>">
            <?= csrf_field() ?>
            <input type="hidden" name="tree" value="<?= e($selected_name) ?>">
            <?php if ($return_url !== '') : ?>
                <input type="hidden" name="return_url" value="<?= e($return_url) ?>">
            <?php endif ?>

            <div class="card mb-3">
                <div class="card-header"><h3 class="h5 mb-0">Email delivery</h3></div>
                <div class="card-body">
                    <div class="alert alert-info">
                        This module sends through the email service configured under
                        <strong>Control panel &gt; Website &gt; Sending email</strong>.
                        Use authenticated SMTP for reliable delivery. Cron controls the time only.
                    </div>

                    <div class="row g-3">
                        <div class="col-12 col-lg-4">
                            <label class="form-label" for="potts-otd-sender-name">Sender name</label>
                            <input class="form-control" id="potts-otd-sender-name" name="sender_name" type="text" value="<?= e($sender_name) ?>" required>
                        </div>
                        <div class="col-12 col-lg-4">
                            <label class="form-label" for="potts-otd-sender-email">Sender email</label>
                            <input class="form-control" id="potts-otd-sender-email" name="sender_email" type="email" value="<?= e($sender_email) ?>" required>
                            <div class="form-text">Use an address accepted by the configured SMTP account.</div>
                        </div>
                        <div class="col-12 col-lg-4">
                            <label class="form-label" for="potts-otd-timezone">Daily timezone</label>
                            <input class="form-control" id="potts-otd-timezone" name="timezone" type="text" value="<?= e($timezone) ?>" placeholder="Australia/Melbourne" required>
                            <div class="form-text">Use a PHP timezone identifier.</div>
                        </div>
                    </div>

                    <div class="d-flex justify-content-end mt-3">
                        <button class="btn btn-primary" type="submit" name="task" value="save">Save settings</button>
                    </div>
                </div>
            </div>

            <div class="card mb-3">
                <div class="card-header"><h3 class="h5 mb-0">Daily scheduler</h3></div>
                <div class="card-body">
                    <p>Cron or another scheduler requests a secure URL once per day. The module then sends messages through webtrees SMTP.</p>
                    <?php if ($has_token) : ?>
                        <button class="btn btn-outline-danger" type="submit" name="task" value="reset_token" onclick="return confirm('Generate a new scheduler URL? The current scheduled task will stop working until it is updated.')">Generate new scheduler token</button>
                    <?php else : ?>
                        <button class="btn btn-success" type="submit" name="task" value="prepare">Prepare secure scheduler link</button>
                    <?php endif ?>
                </div>
            </div>
        </form>

        <?= $scheduler_html ?>

        <div class="card mb-3">
            <div class="card-header"><h3 class="h5 mb-0">Where to create the scheduled task</h3></div>
            <div class="card-body">
                <p>The settings page prepares the secure URL, but it cannot normally create a server-level scheduled task. Choose the option that matches your hosting environment.</p>

                <div class="alert alert-warning">
                    <strong>Check the scheduler timezone.</strong> The module timezone determines which family-tree date is used. The hosting panel, operating system or external service determines the actual time the request runs.
                </div>

                <div class="accordion" id="potts-otd-scheduler-help">
                    <div class="accordion-item">
                        <h4 class="accordion-header" id="potts-otd-help-cpanel-heading">
                            <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#potts-otd-help-cpanel" aria-expanded="true" aria-controls="potts-otd-help-cpanel">cPanel</button>
                        </h4>
                        <div class="accordion-collapse collapse show" id="potts-otd-help-cpanel" aria-labelledby="potts-otd-help-cpanel-heading" data-bs-parent="#potts-otd-scheduler-help">
                            <div class="accordion-body">
                                Open <strong>Advanced &gt; Cron Jobs</strong>, choose a daily time and paste the scheduled task command shown above into the command field.
                            </div>
                        </div>
                    </div>

                    <div class="accordion-item">
                        <h4 class="accordion-header" id="potts-otd-help-plesk-heading">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#potts-otd-help-plesk" aria-expanded="false" aria-controls="potts-otd-help-plesk">Plesk</button>
                        </h4>
                        <div class="accordion-collapse collapse" id="potts-otd-help-plesk" aria-labelledby="potts-otd-help-plesk-heading" data-bs-parent="#potts-otd-scheduler-help">
                            <div class="accordion-body">
                                Open <strong>Websites &amp; Domains &gt; Scheduled Tasks &gt; Add Task</strong>. Choose <strong>Fetch a URL</strong>, paste the secure scheduler URL shown above and select a daily time.
                            </div>
                        </div>
                    </div>

                    <div class="accordion-item">
                        <h4 class="accordion-header" id="potts-otd-help-directadmin-heading">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#potts-otd-help-directadmin" aria-expanded="false" aria-controls="potts-otd-help-directadmin">DirectAdmin</button>
                        </h4>
                        <div class="accordion-collapse collapse" id="potts-otd-help-directadmin" aria-labelledby="potts-otd-help-directadmin-heading" data-bs-parent="#potts-otd-scheduler-help">
                            <div class="accordion-body">
                                Open <strong>User Level &gt; Advanced Features &gt; Cron Jobs</strong>, enter a daily schedule and paste the scheduled task command shown above.
                            </div>
                        </div>
                    </div>

                    <div class="accordion-item">
                        <h4 class="accordion-header" id="potts-otd-help-linux-heading">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#potts-otd-help-linux" aria-expanded="false" aria-controls="potts-otd-help-linux">Linux server with SSH</button>
                        </h4>
                        <div class="accordion-collapse collapse" id="potts-otd-help-linux" aria-labelledby="potts-otd-help-linux-heading" data-bs-parent="#potts-otd-scheduler-help">
                            <div class="accordion-body">
                                Run <code>crontab -e</code> as the web account and add a daily cron expression followed by the command shown above. For example, <code>0 6 * * *</code> runs at 6:00 am in the cron service's timezone.
                            </div>
                        </div>
                    </div>

                    <div class="accordion-item">
                        <h4 class="accordion-header" id="potts-otd-help-external-heading">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#potts-otd-help-external" aria-expanded="false" aria-controls="potts-otd-help-external">No server scheduler available</button>
                        </h4>
                        <div class="accordion-collapse collapse" id="potts-otd-help-external" aria-labelledby="potts-otd-help-external-heading" data-bs-parent="#potts-otd-scheduler-help">
                            <div class="accordion-body">
                                Use a reputable external URL scheduling service. Create a daily HTTPS request and paste the secure scheduler URL shown above. Store the URL as a secret where the service supports secrets and never publish it.
                            </div>
                        </div>
                    </div>

                    <div class="accordion-item">
                        <h4 class="accordion-header" id="potts-otd-help-other-heading">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#potts-otd-help-other" aria-expanded="false" aria-controls="potts-otd-help-other">Windows, NAS or another platform</button>
                        </h4>
                        <div class="accordion-collapse collapse" id="potts-otd-help-other" aria-labelledby="potts-otd-help-other-heading" data-bs-parent="#potts-otd-scheduler-help">
                            <div class="accordion-body">
                                Use the platform's Task Scheduler to run a daily HTTPS request with <code>curl</code>, <code>wget</code> or its built-in URL-fetch action. Use the secure URL shown above and confirm the task reports an HTTP 200 response.
                            </div>
                        </div>
                    </div>
                </div>

                <p class="text-muted mt-3 mb-0">If none of these options is available, ask the hosting provider to schedule one daily HTTPS request to the secure URL.</p>
            </div>
        </div>

        <?= $subscriber_html ?>
    <?php endif ?>
</div>
